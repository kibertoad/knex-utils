const TEST_CONFIG = {
  client: 'dummy',
  connection: {
    host: 'dummy',
    user: 'dummy',
    password: 'dummy',
    database: 'dummy',
  },

  acquireConnectionTimeout: 1000,
  pool: {
    min: 1,
    max: 1
  }
};

module.exports = TEST_CONFIG;
